import { FeeBalancePipe } from './fee-balance.pipe';

describe('FeeBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new FeeBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
